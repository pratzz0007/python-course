# TODO - Fix the code

- [ ] Inspect all existing Python files for syntax/runtime issues.
- [ ] Identify the failing file(s) / expected behavior.
- [ ] Implement fixes (bugs, input handling, indentation, stray prints).
- [ ] Run the scripts (where runnable) to verify behavior.
- [ ] Document how to execute fixed scripts.

